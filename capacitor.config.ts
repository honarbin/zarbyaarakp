import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.honarbin.zarb',
  appName: 'ضربیار',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
